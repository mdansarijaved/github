import React from "react";

function WorkspaceUser({ params }: { params: { userName: string } }) {
  return (
    <div className="w-full h-full bg-white grid justify-center items-center"></div>
  );
}

export default WorkspaceUser;
